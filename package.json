{
    "name": "life-guide-root",
    "version": "1.0.0",
    "description": "Root config to orchestrate frontend and backend for Life Guide App",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm --prefix client start\" \"npm --prefix server run dev\"",
        "install-all": "npm --prefix client install && npm --prefix server install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
